---
phase: 08-parser-reliability-and-llamaparse-byok
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - SlideTutor-AI/src/config/models.ts
  - SlideTutor-AI/src/store/uiStore.ts
  - SlideTutor-AI/src/store/uiStore.test.ts
  - SlideTutor-AI/src/components/SettingsModal.tsx
  - SlideTutor-AI/src/components/SettingsModal.test.tsx
  - SlideTutor-AI/src/lib/api/apiClient.ts
  - SlideTutor-AI/api/lib/env.ts
  - SlideTutor-AI/api/lib/generateService.ts
  - SlideTutor-AI/api/lib/generateService.platform.test.ts
  - SlideTutor-AI/api/lib/parser/provider.ts
  - SlideTutor-AI/api/lib/parser/llamaparseProvider.ts
  - SlideTutor-AI/api/lib/parser/llamaparseProvider.test.ts
autonomous: true
requirements:
  - PARSE-06
must_haves:
  truths:
    - "`My API` users can add a parser configuration without affecting `Platform API`."
    - "`LlamaParse` is integrated through a dedicated adapter path rather than inside the old platform parser quota service."
    - "If `My API` has no parser configured, explain still works through degraded no-parser analysis."
  artifacts:
    - path: "SlideTutor-AI/src/config/models.ts"
      provides: "Persisted parser-BYOK settings shape aligned with existing BYOK model settings."
    - path: "SlideTutor-AI/api/lib/parser/llamaparseProvider.ts"
      provides: "Dedicated `LlamaParse` parser adapter with upload, polling, and result normalization."
    - path: "SlideTutor-AI/api/lib/generateService.ts"
      provides: "Explain-chain wiring that uses `LlamaParse` for `My API` only when parser BYOK is configured."
  key_links:
    - from: "SlideTutor-AI/src/config/models.ts"
      to: "SlideTutor-AI/src/lib/api/apiClient.ts"
      via: "serialized parser-BYOK access payload"
      pattern: "parser"
    - from: "SlideTutor-AI/src/lib/api/apiClient.ts"
      to: "SlideTutor-AI/api/lib/env.ts"
      via: "BYOK parser config passthrough"
      pattern: "llamaparse"
    - from: "SlideTutor-AI/api/lib/generateService.ts"
      to: "SlideTutor-AI/api/lib/parser/llamaparseProvider.ts"
      via: "My API parser resolution"
      pattern: "createLlamaParse"
---

<objective>
Add a dedicated parser BYOK path for `My API`, starting with `LlamaParse`, while preserving the degraded no-parser fallback.

Purpose: let BYOK users bring their own parser cleanly instead of borrowing the platform parser, with a minimal settings surface and a provider adapter that contains `LlamaParse`-specific polling and normalization.
Output: persisted parser BYOK settings, `LlamaParse` adapter/tests, and explain-chain integration that only uses parser BYOK when configured.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-parser-reliability-and-llamaparse-byok/08-CONTEXT.md
@.planning/phases/08-parser-reliability-and-llamaparse-byok/08-RESEARCH.md
@.planning/phases/08-parser-reliability-and-llamaparse-byok/08-VALIDATION.md
@SlideTutor-AI/src/config/models.ts
@SlideTutor-AI/src/store/uiStore.ts
@SlideTutor-AI/src/store/uiStore.test.ts
@SlideTutor-AI/src/components/SettingsModal.tsx
@SlideTutor-AI/src/components/SettingsModal.test.tsx
@SlideTutor-AI/src/lib/api/apiClient.ts
@SlideTutor-AI/api/lib/env.ts
@SlideTutor-AI/api/lib/generateService.ts
@SlideTutor-AI/api/lib/generateService.platform.test.ts
@SlideTutor-AI/api/lib/parser/provider.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persisted `My API` parser configuration and pass it through the BYOK access payload</name>
  <files>SlideTutor-AI/src/config/models.ts, SlideTutor-AI/src/store/uiStore.ts, SlideTutor-AI/src/store/uiStore.test.ts, SlideTutor-AI/src/components/SettingsModal.tsx, SlideTutor-AI/src/components/SettingsModal.test.tsx, SlideTutor-AI/src/lib/api/apiClient.ts, SlideTutor-AI/api/lib/env.ts</files>
  <read_first>
    - SlideTutor-AI/src/config/models.ts
    - SlideTutor-AI/src/store/uiStore.ts
    - SlideTutor-AI/src/store/uiStore.test.ts
    - SlideTutor-AI/src/components/SettingsModal.tsx
    - SlideTutor-AI/src/components/SettingsModal.test.tsx
    - SlideTutor-AI/src/lib/api/apiClient.ts
    - SlideTutor-AI/api/lib/env.ts
    - .planning/phases/08-parser-reliability-and-llamaparse-byok/08-CONTEXT.md
    - .planning/phases/08-parser-reliability-and-llamaparse-byok/08-RESEARCH.md
  </read_first>
  <behavior>
    - `My API` settings include an optional parser configuration.
    - The parser configuration is persisted with the existing BYOK settings model.
    - When parser config is absent, requests omit parser BYOK and later degrade gracefully.
  </behavior>
  <action>Extend `AiAccessSettings` in `SlideTutor-AI/src/config/models.ts` with a dedicated parser section shaped as `parser: { providerId: 'none' | 'llamaparse'; apiKey: string }`. Update normalization defaults so `providerId: 'none'` and an empty API key are the baseline. Persist this shape through `SlideTutor-AI/src/store/uiStore.ts` and add direct normalization/persistence coverage in `SlideTutor-AI/src/store/uiStore.test.ts`. In `SlideTutor-AI/src/components/SettingsModal.tsx`, add a minimal `My API` parser block with one provider option (`LlamaParse`) and an API key input plus concrete helper copy: `Optional. If omitted, SlideTutor uses no-parser degraded analysis.` Update `SlideTutor-AI/src/components/SettingsModal.test.tsx` to cover the new parser UI and persistence. Extend the BYOK request payload in `SlideTutor-AI/src/lib/api/apiClient.ts` and `SlideTutor-AI/api/lib/env.ts` so BYOK access can carry `parser: { providerId: 'llamaparse', apiKey: string }` only when `providerId !== 'none'` and the API key is non-empty.</action>
  <acceptance_criteria>
    - `rg -n "providerId: 'none' \\| 'llamaparse'|parser:" SlideTutor-AI/src/config/models.ts SlideTutor-AI/src/store/uiStore.ts SlideTutor-AI/api/lib/env.ts` finds the new parser settings contract.
    - `rg -n "Optional\\. If omitted, SlideTutor uses no-parser degraded analysis\\.|LlamaParse" SlideTutor-AI/src/components/SettingsModal.tsx` finds the new parser BYOK UI copy.
    - `rg -n "parser" SlideTutor-AI/src/lib/api/apiClient.ts SlideTutor-AI/src/store/uiStore.test.ts SlideTutor-AI/src/components/SettingsModal.test.tsx` finds payload, persistence, and UI coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/store/uiStore.test.ts src/components/SettingsModal.test.tsx src/lib/api/apiClient.test.ts</automated>
  </verify>
  <done>The repo has one explicit, persisted parser BYOK setting for `My API`, and requests can carry `LlamaParse` credentials without changing `Platform API` behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a dedicated `LlamaParse` adapter and wire it into the explain chain</name>
  <files>SlideTutor-AI/api/lib/parser/provider.ts, SlideTutor-AI/api/lib/parser/llamaparseProvider.ts, SlideTutor-AI/api/lib/parser/llamaparseProvider.test.ts, SlideTutor-AI/api/lib/generateService.ts, SlideTutor-AI/api/lib/generateService.platform.test.ts</files>
  <read_first>
    - SlideTutor-AI/api/lib/parser/provider.ts
    - SlideTutor-AI/api/lib/generateService.ts
    - SlideTutor-AI/api/lib/generateService.platform.test.ts
    - SlideTutor-AI/api/lib/parser/volcengineProvider.ts
    - .planning/phases/08-parser-reliability-and-llamaparse-byok/08-CONTEXT.md
    - .planning/phases/08-parser-reliability-and-llamaparse-byok/08-RESEARCH.md
  </read_first>
  <behavior>
    - `My API` requests with parser BYOK configured call `LlamaParse` instead of platform parsing.
    - The `LlamaParse` adapter encapsulates upload, polling, timeout, and result normalization.
    - If `LlamaParse` is not configured, `My API` explain remains degraded instead of failing because the platform parser is unavailable.
  </behavior>
  <action>Create `SlideTutor-AI/api/lib/parser/llamaparseProvider.ts` as a dedicated adapter that uses the official cloud API v2 flow: `POST https://api.cloud.llamaindex.ai/api/v2/parse/upload` with multipart `file` plus JSON `configuration` containing `{ \"tier\": \"cost_effective\", \"version\": \"latest\" }`, then poll `GET https://api.cloud.llamaindex.ai/api/v2/parse/{job_id}?expand=items,metadata,markdown` until the returned `job.status` is `COMPLETED`, `FAILED`, or a bounded timeout is reached. Use a concrete poll budget of 12 attempts with a 2-second delay between attempts. Extend `SlideTutor-AI/api/lib/parser/provider.ts` only as much as needed to pass parser-BYOK config into the adapter. Normalize `LlamaParse` results into the existing explain-chain contract by mapping structured items to `LayoutBlock[]` when coordinates are present; if item geometry is absent or unusable, fall back to one synthetic page block with the page markdown/text and bbox `[0, 0, 1000, 1000]` so the explain pipeline still receives a stable block list. In `SlideTutor-AI/api/lib/generateService.ts`, call the `LlamaParse` adapter only for BYOK requests that include parser config, return degraded analysis when the parser config is absent, and emit explicit BYOK parser error codes when the provider fails or times out. Add `SlideTutor-AI/api/lib/parser/llamaparseProvider.test.ts` and extend `SlideTutor-AI/api/lib/generateService.platform.test.ts` to cover upload request shape, polling completion, timeout/failure paths, and the degraded fallback when parser config is missing.</action>
  <acceptance_criteria>
    - `rg -n "api.cloud.llamaindex.ai/api/v2/parse/upload|api.cloud.llamaindex.ai/api/v2/parse/\\{job_id\\}|cost_effective|latest" SlideTutor-AI/api/lib/parser/llamaparseProvider.ts` finds the official `LlamaParse` API v2 flow.
    - `rg -n "12 attempts|2-second|setTimeout|items,metadata,markdown|COMPLETED|FAILED" SlideTutor-AI/api/lib/parser/llamaparseProvider.ts` finds bounded polling and terminal-status handling.
    - `rg -n "BYOK_PARSER_FAILED|BYOK_PARSER_TIMEOUT|degraded" SlideTutor-AI/api/lib/generateService.ts SlideTutor-AI/api/lib/generateService.platform.test.ts` finds explicit BYOK parser handling.
    - `test -f SlideTutor-AI/api/lib/parser/llamaparseProvider.test.ts` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/lib/parser/llamaparseProvider.test.ts api/lib/generateService.platform.test.ts</automated>
  </verify>
  <done>`My API` has a dedicated `LlamaParse` parser BYOK path with bounded polling, normalized output, and preserved degraded fallback when parser BYOK is absent.</done>
</task>

</tasks>

<verification>
Run `npm test -- src/store/uiStore.test.ts src/components/SettingsModal.test.tsx src/lib/api/apiClient.test.ts api/lib/parser/llamaparseProvider.test.ts api/lib/generateService.platform.test.ts` and `npm run lint`.
</verification>

<success_criteria>
`My API` users can opt into `LlamaParse` cleanly, the adapter hides provider-specific async behavior, and explain still works in degraded mode when no parser BYOK is configured.
</success_criteria>

<output>
After completion, create `.planning/phases/08-parser-reliability-and-llamaparse-byok/08-02-SUMMARY.md`
</output>
