---
phase: 09-model-capability-registry-and-parameter-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SlideTutor-AI/api/lib/modelCapabilities.ts
  - SlideTutor-AI/api/lib/modelCapabilities.test.ts
  - SlideTutor-AI/api/lib/structuredOutputConfig.ts
  - SlideTutor-AI/api/lib/generateService.ts
  - SlideTutor-AI/api/lib/generateService.platform.test.ts
  - SlideTutor-AI/src/config/models.ts
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
must_haves:
  truths:
    - "The repo has one backend-owned capability truth for active AI tasks instead of scattered task/provider assumptions."
    - "Model eligibility is decided against one global hard-constraint baseline rather than separate `My API` / `Platform API` admission rules."
    - "Generation runtime can resolve a model's capability status before normal provider execution begins."
  artifacts:
    - path: "SlideTutor-AI/api/lib/modelCapabilities.ts"
      provides: "Central capability registry, hard/soft constraints, and eligibility helpers."
    - path: "SlideTutor-AI/api/lib/generateService.ts"
      provides: "Runtime preflight that consults capability truth before provider execution."
    - path: "SlideTutor-AI/api/lib/modelCapabilities.test.ts"
      provides: "Regression coverage for capability truth and eligibility decisions."
  key_links:
    - from: "SlideTutor-AI/api/lib/generateService.ts"
      to: "SlideTutor-AI/api/lib/modelCapabilities.ts"
      via: "capability-driven preflight resolution"
      pattern: "resolveModelCapabilityProfile"
    - from: "SlideTutor-AI/api/lib/structuredOutputConfig.ts"
      to: "SlideTutor-AI/api/lib/modelCapabilities.ts"
      via: "active task baseline and constraint mapping"
      pattern: "ACTIVE_MODEL_TASKS"
---

<objective>
Create the backend capability source of truth and wire it into generation preflight.

Purpose: stop relying on scattered task conditionals, frontend model lists, and late provider errors as the de facto model-config truth.
Output: one capability registry module, one global eligibility baseline for active tasks, and runtime preflight that can reject unavailable models before normal execution starts.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-model-capability-registry-and-parameter-hardening/09-CONTEXT.md
@docs/discuss/phases/09-model-capability-registry-and-parameter-hardening-brief.md
@SlideTutor-AI/src/config/models.ts
@SlideTutor-AI/api/lib/env.ts
@SlideTutor-AI/api/lib/structuredOutputConfig.ts
@SlideTutor-AI/api/lib/generateService.ts
@SlideTutor-AI/api/lib/generateService.platform.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a centralized capability registry and global hard/soft constraint baseline</name>
  <files>SlideTutor-AI/api/lib/modelCapabilities.ts, SlideTutor-AI/api/lib/modelCapabilities.test.ts, SlideTutor-AI/api/lib/structuredOutputConfig.ts, SlideTutor-AI/src/config/models.ts</files>
  <read_first>
    - .planning/phases/09-model-capability-registry-and-parameter-hardening/09-CONTEXT.md
    - docs/discuss/phases/09-model-capability-registry-and-parameter-hardening-brief.md
    - SlideTutor-AI/src/config/models.ts
    - SlideTutor-AI/api/lib/structuredOutputConfig.ts
    - SlideTutor-AI/api/lib/generateService.ts
    - docs/backend/platform-model-configuration.md
  </read_first>
  <behavior>
    - The backend defines one active-task baseline and one global model hard-constraint union.
    - Structured-output, streaming, image-input, and provider-specific parameter support are expressed as capability facts instead of scattered conditionals.
    - Frontend-visible model metadata can remain, but it is no longer the capability source of truth.
  </behavior>
  <action>Create `SlideTutor-AI/api/lib/modelCapabilities.ts` and define concrete exports for `ACTIVE_MODEL_TASKS`, `GLOBAL_MODEL_HARD_CONSTRAINTS`, `GLOBAL_MODEL_SOFT_CONSTRAINTS`, `resolveModelCapabilityProfile(...)`, and `isModelEligibleForProduct(...)`. The active task list must be exactly `explain`, `distill`, `followup`, `regenerate_chunk`, `regenerate_followup`, `generate_questions`, and `evaluate_answers`. The global hard-constraint union must include text generation, image input for the active baseline, native structured output for artifact-dependent tasks, and streaming under the current frontend contract; `thinking` must be modeled as soft only. Update `SlideTutor-AI/api/lib/structuredOutputConfig.ts` so task policies can read from the centralized active-task/capability definitions instead of implicitly being the only source of truth. Add `SlideTutor-AI/api/lib/modelCapabilities.test.ts` to assert the exact active task set, hard-vs-soft treatment of `thinking`, and the rule that models failing any hard constraint are not eligible.</action>
  <acceptance_criteria>
    - `rg -n "ACTIVE_MODEL_TASKS|GLOBAL_MODEL_HARD_CONSTRAINTS|GLOBAL_MODEL_SOFT_CONSTRAINTS|resolveModelCapabilityProfile|isModelEligibleForProduct" SlideTutor-AI/api/lib/modelCapabilities.ts` finds the centralized capability API.
    - `rg -n "explain|distill|followup|regenerate_chunk|regenerate_followup|generate_questions|evaluate_answers" SlideTutor-AI/api/lib/modelCapabilities.ts` finds the exact active baseline.
    - `rg -n "thinking.*soft|soft.*thinking|native structured output|streaming|image input" SlideTutor-AI/api/lib/modelCapabilities.ts SlideTutor-AI/api/lib/modelCapabilities.test.ts` finds the hard/soft contract.
    - `npm test -- api/lib/modelCapabilities.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/lib/modelCapabilities.test.ts</automated>
  </verify>
  <done>The repo has one explicit backend capability truth with a stable active-task baseline and hard/soft constraint model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make generation preflight capability-aware and reject globally ineligible models before provider execution</name>
  <files>SlideTutor-AI/api/lib/generateService.ts, SlideTutor-AI/api/lib/generateService.platform.test.ts, SlideTutor-AI/api/lib/modelCapabilities.ts, SlideTutor-AI/api/lib/modelCapabilities.test.ts</files>
  <read_first>
    - SlideTutor-AI/api/lib/generateService.ts
    - SlideTutor-AI/api/lib/generateService.platform.test.ts
    - SlideTutor-AI/api/lib/modelCapabilities.ts
    - .planning/phases/09-model-capability-registry-and-parameter-hardening/09-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Normal generation requests consult capability truth before provider execution begins.
    - Ineligible models are rejected with stable product-facing errors instead of task-specific runtime explosions.
    - Capability preflight remains separate from hosted task-surface policy checks.
  </behavior>
  <action>In `SlideTutor-AI/api/lib/generateService.ts`, add a capability-preflight step before provider execution that resolves the requested model profile and evaluates it against the global hard-constraint baseline. Use explicit runtime errors such as `MODEL_CAPABILITY_UNKNOWN`, `MODEL_CAPABILITY_UNVERIFIED`, or `MODEL_NOT_ELIGIBLE` rather than letting unsupported settings fail later at the provider. Keep this preflight distinct from hosted policy gates such as unsupported hosted tasks. Extend `SlideTutor-AI/api/lib/generateService.platform.test.ts` and `SlideTutor-AI/api/lib/modelCapabilities.test.ts` to cover three concrete cases: a model rejected because hard constraints fail, a model blocked because capability data is missing/stale, and a model allowed through preflight before later task execution.</action>
  <acceptance_criteria>
    - `rg -n "MODEL_CAPABILITY_UNKNOWN|MODEL_CAPABILITY_UNVERIFIED|MODEL_NOT_ELIGIBLE" SlideTutor-AI/api/lib/generateService.ts SlideTutor-AI/api/lib/generateService.platform.test.ts` finds the stable capability-preflight errors.
    - `rg -n "isModelEligibleForProduct|resolveModelCapabilityProfile" SlideTutor-AI/api/lib/generateService.ts` finds capability-aware preflight integration.
    - `npm test -- api/lib/generateService.platform.test.ts api/lib/modelCapabilities.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/lib/generateService.platform.test.ts api/lib/modelCapabilities.test.ts</automated>
  </verify>
  <done>The runtime can reject globally ineligible models before provider calls, and hosted policy remains separated from capability truth.</done>
</task>

</tasks>

<verification>
Run `npm test -- api/lib/modelCapabilities.test.ts api/lib/generateService.platform.test.ts` and `npm run lint`.
</verification>

<success_criteria>
Phase 09 starts from a clean center of gravity: active-task capability truth is centralized, eligibility is global rather than access-mode-specific, and generation preflight no longer depends on late provider failures to discover unsupported models.
</success_criteria>

<output>
After completion, create `.planning/phases/09-model-capability-registry-and-parameter-hardening/09-01-SUMMARY.md`
</output>
