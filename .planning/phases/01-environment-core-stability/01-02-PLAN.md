---
phase: 01-environment-core-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["SlideTutor-AI/api/generate.ts", "SlideTutor-AI/server.ts", "SlideTutor-AI/test_rate_limit.sh"]
autonomous: true
gap_closure: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Making more than 10 requests to the API in 1 minute results in a 429 Too Many Requests status code and rate-limiting headers are visible in the response."
  artifacts:
    - path: "SlideTutor-AI/api/generate.ts"
      provides: "Serverless-ready rate limiting middleware"
      contains: "rateLimit"
    - path: "SlideTutor-AI/test_rate_limit.sh"
      provides: "Automated verification for rate limit behavior"
  key_links:
    - from: "SlideTutor-AI/api/generate.ts"
      to: "express-rate-limit"
      via: "app.use"
      pattern: "rateLimit"
---

<objective>
Close UAT gap: Rate limiting bypass in serverless environment.

Purpose: Ensure rate limiting works correctly when `api/generate.ts` is called directly as a Vercel serverless function, rather than relying on the Express server in `server.ts` which is bypassed on Vercel.
Output: Updated `api/generate.ts` with correct rate limiting logic and `trust proxy` configuration, plus a test script to verify it.
</objective>

<execution_context>
@/Users/qiqicute/Documents/z_cqmeng_file/local_repository/SlideTutor-AI-main/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/qiqicute/Documents/z_cqmeng_file/local_repository/SlideTutor-AI-main/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-environment-core-stability/1-UAT.md
@.planning/phases/01-environment-core-stability/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create rate limiting test script</name>
  <files>SlideTutor-AI/test_rate_limit.sh</files>
  <action>
    Create a simple bash script that starts the local Express server (`npm run dev`) in the background, waits for it to listen, sends 12 requests in a loop using curl to `http://localhost:3000/api/generate` (with empty or test body), checks if any request returns a 429 status code, and then reliably kills the server process. The script should exit with 0 if a 429 was received, otherwise 1. Make the script executable.
  </action>
  <verify>
    <automated>test -f SlideTutor-AI/test_rate_limit.sh</automated>
  </verify>
  <done>Test script exists and is executable.</done>
</task>

<task type="auto">
  <name>Task 1: Fix Rate Limiting in Serverless Environment</name>
  <files>SlideTutor-AI/api/generate.ts, SlideTutor-AI/server.ts</files>
  <action>
    - Ensure `express-rate-limit` logic is cleanly moved from `server.ts` to `api/generate.ts`.
    - Ensure `app.set('trust proxy', 1)` is correctly configured in `api/generate.ts`.
    
    Gap reason: Rate limiting middleware was originally in `server.ts` which is bypassed when `api/generate.ts` is called directly as a serverless function on Vercel. The UAT test found that 13 requests were successfully served within 1 minute.
    Note for executor: `api/generate.ts` may already contain some rate-limit code that is ineffective or improperly configured for Vercel's proxy behavior. Ensure the rate limiter is fully functional and explicitly reads the proxy IP (e.g. `req.headers['x-forwarded-for']`) if `trust proxy` is insufficient, so that IP-based rate limiting is properly applied.
  </action>
  <verify>
    <automated>cd SlideTutor-AI && bash test_rate_limit.sh</automated>
  </verify>
  <done>Making more than 10 requests to the API in 1 minute results in a 429 Too Many Requests status code.</done>
</task>

</tasks>

<verification>
Review UAT gap reason to ensure all missing elements (`trust proxy`, middleware location) are properly addressed in `api/generate.ts` and no longer dependent on `server.ts`.
</verification>

<success_criteria>
Rate limiter successfully blocks requests exceeding the threshold both in local testing and logic review for serverless deployments.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-core-stability/01-environment-core-stability-02-SUMMARY.md`
</output>
