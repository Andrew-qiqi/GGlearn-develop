---
phase: 03-minimal-cloudflare-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - SlideTutor-AI/package.json
  - SlideTutor-AI/src/main.tsx
  - SlideTutor-AI/src/components/SettingsModal.tsx
  - SlideTutor-AI/src/worker/index.ts
  - SlideTutor-AI/src/worker/routes/feedback.ts
  - SlideTutor-AI/wrangler.jsonc
  - SlideTutor-AI/.env.example
  - SlideTutor-AI/README.md
  - SlideTutor-AI/vercel.json
  - docs/architecture/deployment.md
  - docs/backend/api-design.md
  - docs/security/token-authentication.md
autonomous: true
requirements:
  - DEP-01
  - DEP-03
must_haves:
  truths:
    - "The built SPA and the critical-path APIs are intended to ship from one Cloudflare Worker base URL."
    - "Observability and request-protection configuration are documented and wired for the Worker deployment path."
    - "Feedback is explicitly handled during cutover so it is not a hidden Vercel-only dependency."
  artifacts:
    - path: "SlideTutor-AI/src/worker/routes/feedback.ts"
      provides: "Explicit Cloudflare-side handling for `/api/feedback` during migration."
    - path: "SlideTutor-AI/src/main.tsx"
      provides: "Frontend bootstrap free of the Vercel analytics runtime dependency."
    - path: "docs/architecture/deployment.md"
      provides: "Operator-facing Cloudflare deployment and observability instructions."
    - path: "SlideTutor-AI/.env.example"
      provides: "Worker-era env inventory instead of the old Vercel/SMTP assumptions."
  key_links:
    - from: "SlideTutor-AI/src/main.tsx"
      to: "SlideTutor-AI/src/worker/index.ts"
      via: "single public base URL"
      pattern: "createRoot|App"
    - from: "SlideTutor-AI/src/components/SettingsModal.tsx"
      to: "SlideTutor-AI/src/worker/routes/feedback.ts"
      via: "explicit feedback path during migration"
      pattern: "/api/feedback"
    - from: "SlideTutor-AI/wrangler.jsonc"
      to: "docs/architecture/deployment.md"
      via: "deploy and observability configuration"
      pattern: "observability"
---

<objective>
Finish the single-Worker cutover and remove the remaining Vercel-first runtime assumptions from the public deployment path.

Purpose: Make the Cloudflare Worker path the documented deploy target, eliminate silent Vercel-only runtime dependencies, and explicitly resolve `/api/feedback` for the migration window.
Output: Cloudflare-first app bootstrap, explicit feedback handling, updated env/docs, and a final cutover verification checklist.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-minimal-cloudflare-migration/03-CONTEXT.md
@.planning/phases/03-minimal-cloudflare-migration/03-RESEARCH.md
@.planning/phases/03-minimal-cloudflare-migration/03-01-PLAN.md
@.planning/phases/03-minimal-cloudflare-migration/03-02-PLAN.md
@SlideTutor-AI/src/main.tsx
@SlideTutor-AI/src/components/SettingsModal.tsx
@SlideTutor-AI/vercel.json
@SlideTutor-AI/.env.example
@docs/architecture/deployment.md
@docs/backend/api-design.md
@docs/security/token-authentication.md

<interfaces>
From `SlideTutor-AI/src/components/SettingsModal.tsx`:

```ts
const response = await fetch('/api/feedback', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    type,
    reason,
    images,
    contactAgreed,
    email,
  }),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the Cloudflare Worker path the single documented app runtime</name>
  <files>SlideTutor-AI/package.json, SlideTutor-AI/src/main.tsx, SlideTutor-AI/src/worker/index.ts, SlideTutor-AI/wrangler.jsonc, SlideTutor-AI/vercel.json, docs/architecture/deployment.md, docs/backend/api-design.md</files>
  <read_first>
    - SlideTutor-AI/package.json
    - SlideTutor-AI/src/main.tsx
    - SlideTutor-AI/vercel.json
    - SlideTutor-AI/wrangler.jsonc
    - docs/architecture/deployment.md
    - docs/backend/api-design.md
  </read_first>
  <behavior>
    - Test 1: The default documented dev/build/deploy path points to the Worker, not `tsx server.ts`.
    - Test 2: Direct navigation to `/api/*` returns API responses or JSON errors, never the SPA HTML shell.
    - Test 3: The frontend bootstrap no longer imports the Vercel analytics runtime.
  </behavior>
  <action>Update `SlideTutor-AI/package.json` so the primary development and deploy workflow points at the Worker path created in Plans 01-02, not the old Express/Vercel server. Update `SlideTutor-AI/src/main.tsx` to remove `@vercel/analytics/react` so the frontend no longer depends on a Vercel-only runtime helper. Tighten `SlideTutor-AI/src/worker/index.ts` and `SlideTutor-AI/wrangler.jsonc` so API routes always bypass SPA fallback. Delete or retire `SlideTutor-AI/vercel.json` as an active deployment control file. Update `docs/architecture/deployment.md` and `docs/backend/api-design.md` so operators and later phases treat the single Cloudflare Worker plus `/api/generate`, `/api/get-token`, and `/api/parse` as the canonical public base per the Phase 03 decision.</action>
  <acceptance_criteria>
    - `rg -n "@vercel/analytics" SlideTutor-AI/src/main.tsx` returns no matches.
    - `rg -n "\"dev\"|\"dev:cf\"|\"deploy:cf\"" SlideTutor-AI/package.json` shows Cloudflare-first runtime scripts.
    - `rg -n "/api/" SlideTutor-AI/src/worker/index.ts` still shows explicit API dispatch ahead of asset fallback.
    - `rg -n "Cloudflare|Worker|/api/generate|/api/get-token|/api/parse" docs/architecture/deployment.md docs/backend/api-design.md` documents the Cloudflare-first base.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The public deployment path is documented and wired as one Cloudflare Worker serving the SPA and critical-path APIs, with no remaining Vercel analytics/runtime dependency in the frontend bootstrap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve `/api/feedback` explicitly and retire SMTP-era runtime assumptions</name>
  <files>SlideTutor-AI/src/components/SettingsModal.tsx, SlideTutor-AI/src/worker/routes/feedback.ts, SlideTutor-AI/src/worker/index.ts, SlideTutor-AI/.env.example, SlideTutor-AI/README.md, docs/security/token-authentication.md</files>
  <read_first>
    - SlideTutor-AI/src/components/SettingsModal.tsx
    - SlideTutor-AI/api/generate.ts
    - SlideTutor-AI/.env.example
    - SlideTutor-AI/README.md
    - .planning/phases/03-minimal-cloudflare-migration/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `/api/feedback` is handled on Cloudflare rather than silently depending on the old Vercel SMTP path.
    - Test 2: The UI shows an explicit migration-window message instead of an ambiguous retry loop when feedback delivery is deferred.
    - Test 3: The env/docs inventory no longer tells operators to configure SMTP for the public runtime cutover.
  </behavior>
  <action>Create `SlideTutor-AI/src/worker/routes/feedback.ts` as an explicit migration-window route that returns a structured `503` JSON payload such as `{ "error": "Feedback is temporarily unavailable during the Cloudflare migration." }` rather than leaving `/api/feedback` on the old Vercel/SMTP path. Update `SlideTutor-AI/src/worker/index.ts` to route `/api/feedback` through that handler. Update `SlideTutor-AI/src/components/SettingsModal.tsx` so the feedback tab shows a clear Cloudflare-migration notice when the Worker returns that response, instead of implying that email delivery is still live. Remove SMTP-first deployment guidance from `SlideTutor-AI/.env.example`, `SlideTutor-AI/README.md`, and `docs/security/token-authentication.md`; keep token-auth and critical-path API guidance intact, and document that feedback delivery is intentionally deferred outside Phase 03.</action>
  <acceptance_criteria>
    - `rg -n "/api/feedback" SlideTutor-AI/src/worker/index.ts SlideTutor-AI/src/worker/routes/feedback.ts SlideTutor-AI/src/components/SettingsModal.tsx` finds the Cloudflare-side route plus the explicit UI handling.
    - `rg -n "temporarily unavailable|Cloudflare migration" SlideTutor-AI/src/components/SettingsModal.tsx SlideTutor-AI/src/worker/routes/feedback.ts` finds the deferral message in both UI and API layers.
    - `rg -n "SMTP_|SMTP|FEEDBACK_EMAIL" SlideTutor-AI/.env.example SlideTutor-AI/README.md docs/security/token-authentication.md` returns no deployment instructions that keep SMTP as a required public runtime dependency.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workers -- test/workers/spa-routing.worker.test.ts test/workers/generate-stream.worker.test.ts test/workers/security-observability.worker.test.ts</automated>
  </verify>
  <done>The Cloudflare cutover has no accidental `/api/feedback` dependency on Vercel, the UI communicates the deliberate deferment, and operator docs/env samples match the new runtime reality.</done>
</task>

</tasks>

<verification>
Run `npm run build` and `npm run test:workers -- test/workers/spa-routing.worker.test.ts test/workers/generate-stream.worker.test.ts test/workers/security-observability.worker.test.ts`. Smoke-test direct browser navigation to `/api/get-token`, `/api/parse`, `/api/generate`, and `/api/feedback` to confirm API paths do not fall through to `index.html`.
</verification>

<success_criteria>
Phase 03 ends with a single Cloudflare Worker public base, preserved streaming and security behavior for the tutoring critical path, and no hidden Vercel dependency left behind for feedback or frontend bootstrap.
</success_criteria>

<output>
After completion, create `.planning/phases/03-minimal-cloudflare-migration/03-03-SUMMARY.md`
</output>
