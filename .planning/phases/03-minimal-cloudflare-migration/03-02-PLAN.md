---
phase: 03-minimal-cloudflare-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - SlideTutor-AI/api/generate.ts
  - SlideTutor-AI/api/get-token.ts
  - SlideTutor-AI/api/lib/env.ts
  - SlideTutor-AI/api/security.test.ts
  - SlideTutor-AI/src/worker/routes/get-token.ts
  - SlideTutor-AI/src/worker/routes/parse.ts
  - SlideTutor-AI/src/worker/routes/generate.ts
  - SlideTutor-AI/src/worker/lib/streams.ts
  - SlideTutor-AI/src/worker/index.ts
  - SlideTutor-AI/test/workers/generate-stream.worker.test.ts
  - SlideTutor-AI/test/workers/security-observability.worker.test.ts
autonomous: true
requirements:
  - DEP-02
  - DEP-03
must_haves:
  truths:
    - "The Worker serves `/api/get-token`, `/api/parse`, and `/api/generate` without sending those requests back to Vercel or Express."
    - "The `/api/generate` response still streams plain-text chunks in the same order the frontend hooks expect."
    - "Missing route secrets and token-auth failures return route-specific errors instead of crashing the Worker at startup."
  artifacts:
    - path: "SlideTutor-AI/src/worker/routes/generate.ts"
      provides: "Worker-native `/api/generate` route with streaming response handling."
    - path: "SlideTutor-AI/src/worker/routes/get-token.ts"
      provides: "Worker-native `/api/get-token` route preserving token shape and rate limiting."
    - path: "SlideTutor-AI/src/worker/routes/parse.ts"
      provides: "Worker-native `/api/parse` route preserving Azure layout response shape."
    - path: "SlideTutor-AI/test/workers/generate-stream.worker.test.ts"
      provides: "Worker-runtime streaming coverage for explain/distill generation flow."
  key_links:
    - from: "SlideTutor-AI/src/worker/routes/generate.ts"
      to: "SlideTutor-AI/api/lib/structuredOutputConfig.ts"
      via: "provider generation config reuse"
      pattern: "buildGeminiGenerationConfig|buildOpenAICompatibleGenerationConfig"
    - from: "SlideTutor-AI/src/worker/routes/generate.ts"
      to: "SlideTutor-AI/src/worker/lib/streams.ts"
      via: "ReadableStream response conversion"
      pattern: "ReadableStream"
    - from: "SlideTutor-AI/src/worker/routes/parse.ts"
      to: "SlideTutor-AI/api/generate.ts"
      via: "shared Azure layout analysis logic"
      pattern: "performAzureAnalysis"
---

<objective>
Move the first-release critical-path APIs onto Worker-native handlers without changing teaching logic or frontend contracts.

Purpose: Preserve the existing tutoring pipeline and request/response contracts while swapping the Node/Express shell for Worker request handlers.
Output: Worker routes for `/api/get-token`, `/api/parse`, and `/api/generate`, plus passing route-scoped env and streaming tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-minimal-cloudflare-migration/03-CONTEXT.md
@.planning/phases/03-minimal-cloudflare-migration/03-RESEARCH.md
@.planning/phases/03-minimal-cloudflare-migration/03-01-PLAN.md
@SlideTutor-AI/api/generate.ts
@SlideTutor-AI/api/get-token.ts
@SlideTutor-AI/api/lib/env.ts
@SlideTutor-AI/api/lib/tokenAuth.ts
@SlideTutor-AI/api/lib/structuredOutputConfig.ts
@SlideTutor-AI/src/lib/api/apiClient.ts
@SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts
@SlideTutor-AI/src/hooks/useChunkRegenerate.test.ts

<interfaces>
From `SlideTutor-AI/src/lib/api/apiClient.ts`:

```ts
export async function apiGenerate(
  params: {
    providerId: string;
    modelId: string;
    base64Image?: string;
    textContent?: string;
    systemInstruction?: string;
    task: string;
    taskData?: any;
  },
  signal?: AbortSignal
): Promise<Response>;
```

From `SlideTutor-AI/api/lib/tokenAuth.ts`:

```ts
export function generateToken(): string;
export function verifyToken(token: string | undefined): {
  valid: boolean;
  error?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract route-scoped service entrypoints from the current Express handlers</name>
  <files>SlideTutor-AI/api/generate.ts, SlideTutor-AI/api/get-token.ts, SlideTutor-AI/api/lib/env.ts, SlideTutor-AI/api/security.test.ts</files>
  <read_first>
    - SlideTutor-AI/api/generate.ts
    - SlideTutor-AI/api/get-token.ts
    - SlideTutor-AI/api/lib/env.ts
    - SlideTutor-AI/api/security.test.ts
    - .planning/phases/03-minimal-cloudflare-migration/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `/api/get-token` requires `API_TOKEN_SECRET` only when token generation is actually invoked.
    - Test 2: `/api/parse` requires Azure credentials only when parse is requested.
    - Test 3: `/api/generate` requires only the provider credentials needed for the chosen provider/task and keeps token-auth semantics unchanged.
  </behavior>
  <action>Refactor `SlideTutor-AI/api/generate.ts` and `SlideTutor-AI/api/get-token.ts` so the runtime-neutral logic is exportable to Worker handlers: keep `buildPrompt`, structured-output config usage, `performAzureAnalysis`, malicious-intent checks, provider routing, and token format unchanged, but remove the module-level `validateEnv()` hard fail. Replace `SlideTutor-AI/api/lib/env.ts` with route-scoped capability helpers that can validate Gemini, OpenAI-compatible provider keys, Azure parser keys, and `API_TOKEN_SECRET` on demand. Update `SlideTutor-AI/api/security.test.ts` so Phase 03 verifies route-scoped capability behavior instead of startup-wide environment failure. Per D-runtime-adaptation, do not alter teaching prompt intent, artifact schemas, or frontend-facing JSON/text response shapes.</action>
  <acceptance_criteria>
    - `rg -n "validateEnv\\(" SlideTutor-AI/api/generate.ts` does not show a top-level startup call any longer.
    - `rg -n "requireSecret|requireAnySecret|requireProvider" SlideTutor-AI/api/lib/env.ts` finds route-scoped capability helpers used by the migrated services.
    - `rg -n "route-scoped|provider|parse" SlideTutor-AI/api/security.test.ts` shows tests for provider-specific and parser-specific env validation.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/security.test.ts</automated>
  </verify>
  <done>Existing service logic can be called from Worker routes without Express globals, and env validation now matches the BYOK-friendly route-scoped model recommended in research.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Worker-native `/api/get-token`, `/api/parse`, and `/api/generate` handlers</name>
  <files>SlideTutor-AI/src/worker/routes/get-token.ts, SlideTutor-AI/src/worker/routes/parse.ts, SlideTutor-AI/src/worker/routes/generate.ts, SlideTutor-AI/src/worker/lib/streams.ts, SlideTutor-AI/src/worker/index.ts, SlideTutor-AI/test/workers/generate-stream.worker.test.ts, SlideTutor-AI/test/workers/security-observability.worker.test.ts</files>
  <read_first>
    - SlideTutor-AI/src/worker/index.ts
    - SlideTutor-AI/api/generate.ts
    - SlideTutor-AI/api/get-token.ts
    - SlideTutor-AI/src/lib/api/apiClient.ts
    - SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts
    - SlideTutor-AI/src/hooks/useChunkRegenerate.test.ts
  </read_first>
  <behavior>
    - Test 1: `/api/get-token` returns `{ token, expiresIn: 300 }` and enforces the same 20-per-minute abuse-control boundary.
    - Test 2: `/api/parse` returns `{ blocks: [...] }` with the existing Azure-derived block shape.
    - Test 3: `/api/generate` streams plain text chunks for explain and distill flows without changing frontend consumption semantics.
    - Test 4: Token-auth failures and provider/env failures return route-specific JSON errors instead of HTML or startup crashes.
  </behavior>
  <action>Create `SlideTutor-AI/src/worker/routes/get-token.ts`, `SlideTutor-AI/src/worker/routes/parse.ts`, and `SlideTutor-AI/src/worker/routes/generate.ts` as Worker handlers that wrap the extracted logic from Task 1. Create `SlideTutor-AI/src/worker/lib/streams.ts` to convert async provider output into Worker `ReadableStream` responses instead of `res.write()`. Update `SlideTutor-AI/src/worker/index.ts` to dispatch the three routes from one public base URL. Preserve the current `/api/get-token` JSON contract, `/api/parse` JSON contract, and `/api/generate` text-stream contract exactly; do not change `buildPrompt`, structured output configuration, frontend request payloads, or the provider-family split in `src/config/models.ts`. Add `SlideTutor-AI/test/workers/generate-stream.worker.test.ts` plus coverage updates in `SlideTutor-AI/test/workers/security-observability.worker.test.ts` for token auth, Azure parse capability gating, and streamed generate responses.</action>
  <acceptance_criteria>
    - `rg -n "new Response|ReadableStream" SlideTutor-AI/src/worker/routes/generate.ts SlideTutor-AI/src/worker/lib/streams.ts` shows Worker-native stream responses instead of `res.write`.
    - `rg -n "\"expiresIn\": 300|expiresIn: 300" SlideTutor-AI/src/worker/routes/get-token.ts` shows the unchanged token response shape.
    - `rg -n "performAzureAnalysis|blocks" SlideTutor-AI/src/worker/routes/parse.ts` shows Azure parse reuse with the existing response contract.
    - `rg -n "stream|401|TOKEN" SlideTutor-AI/test/workers/generate-stream.worker.test.ts SlideTutor-AI/test/workers/security-observability.worker.test.ts` finds Worker coverage for DEP-02 and DEP-03 behaviors.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workers -- test/workers/generate-stream.worker.test.ts test/workers/security-observability.worker.test.ts</automated>
  </verify>
  <done>The first-release critical-path APIs run on Worker-native handlers and pass Worker-runtime streaming/security tests without changing teaching logic or frontend contracts.</done>
</task>

</tasks>

<verification>
Run `npm test -- api/security.test.ts` and `npm run test:workers -- test/workers/generate-stream.worker.test.ts test/workers/security-observability.worker.test.ts`. Confirm `src/hooks/useSlideAnalysis.test.ts` and `src/hooks/useChunkRegenerate.test.ts` still pass if they are touched by any response-shape changes.
</verification>

<success_criteria>
`/api/get-token`, `/api/parse`, and `/api/generate` now execute on the Cloudflare Worker path, streaming and security behavior remain intact, and no startup-wide env hard fail blocks the public runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/03-minimal-cloudflare-migration/03-02-SUMMARY.md`
</output>
