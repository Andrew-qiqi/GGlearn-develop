---
phase: 03-minimal-cloudflare-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SlideTutor-AI/package.json
  - SlideTutor-AI/vite.config.ts
  - SlideTutor-AI/wrangler.jsonc
  - SlideTutor-AI/src/worker/index.ts
  - SlideTutor-AI/src/worker/lib/env.ts
  - SlideTutor-AI/src/worker/lib/ip.ts
  - SlideTutor-AI/src/worker/lib/rate-limit.ts
  - SlideTutor-AI/src/worker/lib/observability.ts
  - SlideTutor-AI/test/workers/spa-routing.worker.test.ts
  - SlideTutor-AI/test/workers/security-observability.worker.test.ts
autonomous: true
requirements:
  - DEP-01
  - DEP-03
must_haves:
  truths:
    - "The project can run a Cloudflare Worker dev/build path without Express being the primary public runtime."
    - "SPA routes and /api/* routes share one Worker entrypoint, and /api/* paths win before index.html fallback."
    - "Route handlers can request only the secrets and protection bindings they need instead of failing at module startup."
  artifacts:
    - path: "SlideTutor-AI/wrangler.jsonc"
      provides: "Single-Worker deploy config with static assets, node compatibility, and observability."
    - path: "SlideTutor-AI/src/worker/index.ts"
      provides: "Fetch entrypoint with API-path dispatch before SPA fallback."
    - path: "SlideTutor-AI/src/worker/lib/env.ts"
      provides: "Route-scoped capability validation helpers for Worker bindings."
    - path: "SlideTutor-AI/test/workers/spa-routing.worker.test.ts"
      provides: "Worker-runtime routing coverage for SPA fallback versus API precedence."
  key_links:
    - from: "SlideTutor-AI/vite.config.ts"
      to: "SlideTutor-AI/src/worker/index.ts"
      via: "@cloudflare/vite-plugin Worker entry"
      pattern: "cloudflare|worker"
    - from: "SlideTutor-AI/wrangler.jsonc"
      to: "SlideTutor-AI/dist"
      via: "assets.directory"
      pattern: "\"directory\"\\s*:\\s*\"\\./dist\""
    - from: "SlideTutor-AI/src/worker/index.ts"
      to: "SlideTutor-AI/test/workers/spa-routing.worker.test.ts"
      via: "Worker fetch routing"
      pattern: "/api/"
---

<objective>
Establish the Cloudflare Worker foundation for Phase 03 without moving teaching logic yet.

Purpose: Create the single-Worker shell, route-scoped runtime helpers, and Worker-runtime tests that every later migration task will build on.
Output: Worker config, Worker entrypoint, protection/observability helper modules, and passing routing/security foundation tests.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-minimal-cloudflare-migration/03-CONTEXT.md
@.planning/phases/03-minimal-cloudflare-migration/03-RESEARCH.md
@SlideTutor-AI/package.json
@SlideTutor-AI/vite.config.ts
@SlideTutor-AI/server.ts
@SlideTutor-AI/api/generate.ts
@SlideTutor-AI/api/get-token.ts

<interfaces>
From `SlideTutor-AI/api/lib/tokenAuth.ts`:

```ts
export function generateToken(): string;
export function verifyToken(token: string | undefined): {
  valid: boolean;
  error?: string;
};
```

From `SlideTutor-AI/api/lib/structuredOutputConfig.ts`:

```ts
export function buildGeminiGenerationConfig({ task }: { task: string }): Record<string, unknown>;
export function buildOpenAICompatibleGenerationConfig({ task }: { task: string }): Record<string, unknown>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the Cloudflare Worker runtime shell and toolchain</name>
  <files>SlideTutor-AI/package.json, SlideTutor-AI/vite.config.ts, SlideTutor-AI/wrangler.jsonc, SlideTutor-AI/src/worker/index.ts</files>
  <read_first>
    - SlideTutor-AI/package.json
    - SlideTutor-AI/vite.config.ts
    - SlideTutor-AI/server.ts
    - .planning/phases/03-minimal-cloudflare-migration/03-CONTEXT.md
    - .planning/phases/03-minimal-cloudflare-migration/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Direct requests to `/api/*` hit Worker route dispatch before SPA asset fallback.
    - Test 2: Non-API navigation falls back to the built SPA entry file from the same Worker.
  </behavior>
  <action>Add `wrangler`, `@cloudflare/vite-plugin`, `@cloudflare/workers-types`, and `@cloudflare/vitest-pool-workers` to `SlideTutor-AI/package.json`, plus scripts `dev:cf`, `build:cf`, `deploy:cf`, and `test:workers`. Update `SlideTutor-AI/vite.config.ts` so Cloudflare Vite development runs in `workerd` while the existing `jsdom` frontend tests still use `src/test/setup.ts`. Create `SlideTutor-AI/wrangler.jsonc` with `compatibility_date: "2026-04-03"`, `compatibility_flags: ["nodejs_compat"]`, `assets.directory: "./dist"`, `assets.not_found_handling: "single-page-application"`, and `observability.logs.enabled: true` plus `observability.traces.enabled: true`. Create `SlideTutor-AI/src/worker/index.ts` as the Worker fetch entrypoint that routes `/api/*` first, serves static assets from the same deployment for everything else, and leaves teaching/business logic untouched per the locked Phase 03 boundary.</action>
  <acceptance_criteria>
    - `rg -n "\"dev:cf\"|\"build:cf\"|\"deploy:cf\"|\"test:workers\"" SlideTutor-AI/package.json` finds the four Cloudflare scripts.
    - `rg -n "\"nodejs_compat\"|\"single-page-application\"|\"observability\"" SlideTutor-AI/wrangler.jsonc` finds the Worker compatibility, SPA fallback, and observability settings.
    - `rg -n "/api/" SlideTutor-AI/src/worker/index.ts` shows explicit API routing before SPA fallback logic.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>The repo has a primary Cloudflare Worker runtime shell, Worker-aware Vite config, and deploy/test scripts ready for API migration work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route-scoped env, IP, rate-limit, and observability foundation with Worker tests</name>
  <files>SlideTutor-AI/src/worker/lib/env.ts, SlideTutor-AI/src/worker/lib/ip.ts, SlideTutor-AI/src/worker/lib/rate-limit.ts, SlideTutor-AI/src/worker/lib/observability.ts, SlideTutor-AI/test/workers/spa-routing.worker.test.ts, SlideTutor-AI/test/workers/security-observability.worker.test.ts</files>
  <read_first>
    - SlideTutor-AI/api/lib/env.ts
    - SlideTutor-AI/api/security.test.ts
    - SlideTutor-AI/api/generate.ts
    - SlideTutor-AI/api/get-token.ts
    - .planning/phases/03-minimal-cloudflare-migration/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Missing secrets fail only on the route that needs them.
    - Test 2: IP extraction prefers `CF-Connecting-IP`, then `X-Forwarded-For`, then `X-Real-IP`.
    - Test 3: Worker request logging emits request id, path, status, and duration fields needed for DEP-03.
  </behavior>
  <action>Create `SlideTutor-AI/src/worker/lib/env.ts` with route-scoped helpers such as `requireSecret`, `requireAnySecret`, and `requireBooleanFlag` so `/api/generate`, `/api/get-token`, and `/api/parse` can validate only their own capabilities instead of calling `validateEnv()` at module load. Create `SlideTutor-AI/src/worker/lib/ip.ts` to normalize `CF-Connecting-IP`, `X-Forwarded-For`, and `X-Real-IP`. Create `SlideTutor-AI/src/worker/lib/rate-limit.ts` as a Worker adapter that can use a Cloudflare binding in production and a deterministic in-memory fallback in tests. Create `SlideTutor-AI/src/worker/lib/observability.ts` for request id creation plus structured log lines containing `requestId`, `path`, `status`, `durationMs`, `providerId`, and `task` where available. Add `SlideTutor-AI/test/workers/spa-routing.worker.test.ts` and `SlideTutor-AI/test/workers/security-observability.worker.test.ts` to prove API-route precedence, route-scoped secret failure, IP extraction order, and structured log output; do not port the actual generation/parse handlers yet.</action>
  <acceptance_criteria>
    - `rg -n "requireSecret|requireAnySecret|requireBooleanFlag" SlideTutor-AI/src/worker/lib/env.ts` finds route-scoped capability helpers.
    - `rg -n "CF-Connecting-IP|X-Forwarded-For|X-Real-IP" SlideTutor-AI/src/worker/lib/ip.ts` shows the header priority order.
    - `rg -n "requestId|durationMs|status" SlideTutor-AI/src/worker/lib/observability.ts` finds structured observability fields.
    - `rg -n "route-scoped|CF-Connecting-IP|requestId" SlideTutor-AI/test/workers/security-observability.worker.test.ts` finds coverage for DEP-03 concerns.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:workers -- test/workers/spa-routing.worker.test.ts test/workers/security-observability.worker.test.ts</automated>
  </verify>
  <done>Worker foundation helpers exist, their core behaviors are proven in Worker-runtime tests, and later plans can migrate handlers without inventing new runtime primitives.</done>
</task>

</tasks>

<verification>
Run `npm run lint` and `npm run test:workers -- test/workers/spa-routing.worker.test.ts test/workers/security-observability.worker.test.ts`. Confirm the Worker shell and helpers pass before any API handler migration begins.
</verification>

<success_criteria>
Phase 03 has a working Worker shell, Cloudflare-native runtime helpers, and passing Worker tests that define the migration contracts for SPA routing and DEP-03 protections.
</success_criteria>

<output>
After completion, create `.planning/phases/03-minimal-cloudflare-migration/03-01-SUMMARY.md`
</output>
