---
phase: 05-parser-bootstrap-and-provider-abstraction
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - SlideTutor-AI/src/types.ts
  - SlideTutor-AI/src/store/tutorStore.ts
  - SlideTutor-AI/src/hooks/useSlideAnalysis.ts
  - SlideTutor-AI/src/components/SettingsModal.tsx
  - SlideTutor-AI/src/components/CanvasTutor.tsx
  - SlideTutor-AI/src/components/SettingsModal.test.tsx
  - SlideTutor-AI/src/components/CanvasTutor.test.tsx
  - SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts
  - SlideTutor-AI/src/lib/api/apiClient.ts
  - SlideTutor-AI/README.md
  - docs/backend/api-design.md
  - docs/frontend/architecture.md
  - docs/frontend/data-flow.md
  - docs/changelog/CHANGELOG_TECH.md
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
must_haves:
  truths:
    - "Users can see exact parser usage in Settings as `x/10` without a persistent quota banner in the main reading flow."
    - "Only analyses actually downgraded by the server show the orange `Low accuracy` warning and hover detail."
    - "Frontend state reads parser downgrade truth from server metadata rather than inferring it locally."
  artifacts:
    - path: "SlideTutor-AI/src/components/SettingsModal.tsx"
      provides: "Settings-only parser usage display in the AI Settings area."
    - path: "SlideTutor-AI/src/hooks/useSlideAnalysis.ts"
      provides: "Streaming analysis flow that reads parser downgrade headers and persists per-page accuracy metadata."
    - path: "SlideTutor-AI/src/components/CanvasTutor.tsx"
      provides: "Lightweight downgraded-analysis warning with the agreed product copy."
    - path: "docs/backend/api-design.md"
      provides: "Documented parser usage endpoint and generate-response downgrade metadata."
---

<objective>
Complete the user-facing half of Phase 05 so parser limits are visible in Settings and downgraded analyses communicate lower precision without disrupting the normal study flow.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-VALIDATION.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-01-PLAN.md
@SlideTutor-AI/src/types.ts
@SlideTutor-AI/src/store/tutorStore.ts
@SlideTutor-AI/src/hooks/useSlideAnalysis.ts
@SlideTutor-AI/src/components/SettingsModal.tsx
@SlideTutor-AI/src/components/CanvasTutor.tsx
@SlideTutor-AI/src/lib/api/apiClient.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add settings usage fetching and exact `Document Parsing 7/10` display</name>
  <files>SlideTutor-AI/src/lib/api/apiClient.ts, SlideTutor-AI/src/components/SettingsModal.tsx, SlideTutor-AI/src/components/SettingsModal.test.tsx</files>
  <read_first>
    - SlideTutor-AI/src/lib/api/apiClient.ts
    - SlideTutor-AI/src/components/SettingsModal.tsx
    - SlideTutor-AI/src/components/SettingsModal.test.tsx
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the AI Settings area fetches parser usage from `/api/parser-usage` and renders exact `used/limit` text such as `7/10`.
    - Test 2: the usage row uses product-facing `Document Parsing` copy and does not mention `Azure`.
    - Test 3: the settings UI remains lightweight and does not introduce a persistent quota warning outside Settings.
  </behavior>
  <action>Add a small API helper in `SlideTutor-AI/src/lib/api/apiClient.ts` for `GET /api/parser-usage` that returns `{ used, remaining, limit }`. Update `SlideTutor-AI/src/components/SettingsModal.tsx` so the AI Settings area fetches this summary when the modal is open on the `ai` tab, then renders a lightweight row labeled `Document Parsing` with exact numeric text like `7/10`. Keep the row inside the existing AI Settings content and do not add any always-visible quota banner near the main Analyze flow. Extend `SlideTutor-AI/src/components/SettingsModal.test.tsx` to assert the exact `Document Parsing` copy, exact `7/10` format, and absence of `Azure` product copy.</action>
  <acceptance_criteria>
    - `rg -n "parser-usage|used|remaining|limit" SlideTutor-AI/src/lib/api/apiClient.ts` finds the settings usage helper.
    - `rg -n "Document Parsing|7/10|parser usage" SlideTutor-AI/src/components/SettingsModal.tsx` finds the exact usage row content in AI Settings.
    - `rg -n "Document Parsing|7/10|Azure" SlideTutor-AI/src/components/SettingsModal.test.tsx` finds assertions for product wording and exact usage formatting.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/SettingsModal.test.tsx</automated>
  </verify>
  <done>The Settings modal can show the exact daily parser usage from server truth in the intended low-anxiety location.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist downgraded-analysis accuracy state and render the `Low accuracy` warning</name>
  <files>SlideTutor-AI/src/types.ts, SlideTutor-AI/src/store/tutorStore.ts, SlideTutor-AI/src/hooks/useSlideAnalysis.ts, SlideTutor-AI/src/components/CanvasTutor.tsx, SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts, SlideTutor-AI/src/components/CanvasTutor.test.tsx</files>
  <read_first>
    - SlideTutor-AI/src/types.ts
    - SlideTutor-AI/src/store/tutorStore.ts
    - SlideTutor-AI/src/hooks/useSlideAnalysis.ts
    - SlideTutor-AI/src/components/CanvasTutor.tsx
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `useSlideAnalysis` reads `x-slidetutor-parse-mode` from `/api/generate` and sets page-level accuracy state before streaming completes.
    - Test 2: only `degraded` responses produce `Low accuracy`; normal analyses do not show the warning.
    - Test 3: the hover detail exactly matches `Document parsing is unavailable for this analysis, so precision may be lower.`
  </behavior>
  <action>Extend `SlideTutor-AI/src/types.ts` and `SlideTutor-AI/src/store/tutorStore.ts` with page-state fields such as `analysisAccuracy?: "normal" | "low"` and `analysisAccuracyReason?: "parser-unavailable"`. Update `SlideTutor-AI/src/hooks/useSlideAnalysis.ts` so it reads `x-slidetutor-parse-mode` from the explain response headers before consuming the stream and stores `analysisAccuracy: "low"` plus `analysisAccuracyReason: "parser-unavailable"` when the value is `degraded`; otherwise set `analysisAccuracy: "normal"` and clear the reason. Update `SlideTutor-AI/src/components/CanvasTutor.tsx` so downgraded analyses render a light orange inline warning with the short text `Low accuracy` and a hover tooltip containing `Document parsing is unavailable for this analysis, so precision may be lower.` Place the warning in the result area only when the current analysis is actually downgraded. Extend `SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts` and `SlideTutor-AI/src/components/CanvasTutor.test.tsx` to cover both degraded and normal cases.</action>
  <acceptance_criteria>
    - `rg -n "analysisAccuracy|analysisAccuracyReason|parser-unavailable" SlideTutor-AI/src/types.ts SlideTutor-AI/src/store/tutorStore.ts` finds the new per-page accuracy fields.
    - `rg -n "x-slidetutor-parse-mode|degraded|analysisAccuracy" SlideTutor-AI/src/hooks/useSlideAnalysis.ts` finds header-driven accuracy handling.
    - `rg -n "Low accuracy|Document parsing is unavailable for this analysis, so precision may be lower." SlideTutor-AI/src/components/CanvasTutor.tsx` finds the exact warning and hover detail strings.
    - `rg -n "degraded|Low accuracy|parser-unavailable" SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts SlideTutor-AI/src/components/CanvasTutor.test.tsx` finds automated coverage for the downgrade UX.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/hooks/useSlideAnalysis.test.ts src/components/CanvasTutor.test.tsx</automated>
  </verify>
  <done>The frontend truthfully reflects downgraded analyses and only surfaces the warning when the server actually ran without document parsing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document the parser quota contract, routes, and downgrade UX</name>
  <files>SlideTutor-AI/README.md, docs/backend/api-design.md, docs/frontend/architecture.md, docs/frontend/data-flow.md, docs/changelog/CHANGELOG_TECH.md</files>
  <read_first>
    - SlideTutor-AI/README.md
    - docs/backend/api-design.md
    - docs/frontend/architecture.md
    - docs/frontend/data-flow.md
    - docs/changelog/CHANGELOG_TECH.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
  </read_first>
  <behavior>
    - Docs describe platform-managed `Document Parsing`, the `10/day` rule, successful-only deduction, and degrade-not-block behavior.
    - Backend docs name `/api/parser-usage` and `x-slidetutor-parse-mode`.
    - Frontend docs explain the Settings-only quota display and downgraded-analysis warning path.
  </behavior>
  <action>Update `SlideTutor-AI/README.md` with the current product posture: BYOK is free, `Document Parsing` is platform-funded early, daily parser usage is limited to `10`, and over-quota analysis continues with lower precision. Update `docs/backend/api-design.md` to document the `GET /api/parser-usage` route, its `{ used, remaining, limit }` JSON shape, and the `x-slidetutor-parse-mode` response header on `/api/generate`. Update `docs/frontend/architecture.md` and `docs/frontend/data-flow.md` to explain the Settings-only usage display and the per-analysis downgraded-accuracy state path. Add a concise technical changelog entry in `docs/changelog/CHANGELOG_TECH.md` describing the D1-backed parser quota truth, parser abstraction layer, and `Low accuracy` UX contract.</action>
  <acceptance_criteria>
    - `rg -n "Document Parsing|10/day|Low accuracy|parser-usage|x-slidetutor-parse-mode" SlideTutor-AI/README.md docs/backend/api-design.md docs/frontend/architecture.md docs/frontend/data-flow.md docs/changelog/CHANGELOG_TECH.md` finds the documented contract across product, backend, and frontend docs.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run test:workers</automated>
  </verify>
  <done>The new parser quota and downgrade behavior are documented clearly enough for future execution, verification, and onboarding.</done>
</task>

</tasks>

<verification>
Run `npm test -- src/components/SettingsModal.test.tsx src/hooks/useSlideAnalysis.test.ts src/components/CanvasTutor.test.tsx` and then `npm test && npm run test:workers`.
</verification>

<success_criteria>
Phase 05 exposes parser usage in the intended Settings location, communicates downgraded analyses honestly and lightly, and documents the new backend/frontend contract without disturbing the main learning flow.
</success_criteria>

<output>
After completion, create `.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-02-SUMMARY.md`
</output>
