---
phase: 05-parser-bootstrap-and-provider-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SlideTutor-AI/wrangler.jsonc
  - SlideTutor-AI/src/worker/index.ts
  - SlideTutor-AI/src/worker/routes/generate.ts
  - SlideTutor-AI/src/worker/routes/parse.ts
  - SlideTutor-AI/src/worker/routes/parser-usage.ts
  - SlideTutor-AI/api/lib/env.ts
  - SlideTutor-AI/api/lib/generateService.ts
  - SlideTutor-AI/api/lib/parser/provider.ts
  - SlideTutor-AI/api/lib/parser/azureProvider.ts
  - SlideTutor-AI/api/lib/parser/usageIdentity.ts
  - SlideTutor-AI/api/lib/parser/usageStore.ts
  - SlideTutor-AI/api/lib/parser/accessService.ts
  - SlideTutor-AI/api/parserUsage.test.ts
  - SlideTutor-AI/api/parserAccess.test.ts
  - SlideTutor-AI/test/workers/parse-route.worker.test.ts
  - SlideTutor-AI/migrations/001_parser_usage_daily.sql
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
must_haves:
  truths:
    - "Platform-managed document parsing still works with no user parser setup, but successful parser calls are now explicitly limited and observable."
    - "Parser quota truth lives server-side in Cloudflare D1 and only increments after a successful platform parser call."
    - "Azure remains only an internal provider implementation behind a parser abstraction boundary shared by `/api/parse` and integrated explain parsing."
    - "Over-quota explain requests degrade instead of blocking, and the server emits explicit downgraded-analysis metadata."
  artifacts:
    - path: "SlideTutor-AI/api/lib/parser/accessService.ts"
      provides: "Shared parser access layer that applies quota checks, provider selection, and degrade semantics."
    - path: "SlideTutor-AI/api/lib/parser/usageStore.ts"
      provides: "D1-backed daily parser usage truth with successful-only increments."
    - path: "SlideTutor-AI/src/worker/routes/parser-usage.ts"
      provides: "Settings-facing usage summary endpoint for exact `x/10` display."
    - path: "SlideTutor-AI/src/worker/routes/generate.ts"
      provides: "Streaming generate route that emits parser downgrade metadata without changing the stream body contract."
  key_links:
    - from: "SlideTutor-AI/src/worker/routes/parse.ts"
      to: "SlideTutor-AI/api/lib/parser/accessService.ts"
      via: "shared parser access path"
      pattern: "resolveParserAccess|performDocumentParsing|getParserUsageSummary"
    - from: "SlideTutor-AI/api/lib/generateService.ts"
      to: "SlideTutor-AI/api/lib/parser/accessService.ts"
      via: "integrated explain parsing"
      pattern: "parseMode|degraded|layoutBlocks"
    - from: "SlideTutor-AI/wrangler.jsonc"
      to: "SlideTutor-AI/migrations/001_parser_usage_daily.sql"
      via: "D1 binding + migration"
      pattern: "d1_databases|PARSER_USAGE_DB"
---

<objective>
Create the backend half of Phase 05 so parser usage becomes explicit, limited, and provider-abstracted without changing mature teaching logic.

Purpose: add D1-backed quota truth, a minimal parser abstraction boundary, a shared access layer for `/api/parse` and integrated explain parsing, and downgrade metadata for over-quota analysis.
Output: D1 binding/migration, parser usage service, parser provider interface with Azure implementation, new parser-usage endpoint, and tests for successful-only deduction plus degrade-not-block behavior.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
@.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-VALIDATION.md
@SlideTutor-AI/wrangler.jsonc
@SlideTutor-AI/src/worker/index.ts
@SlideTutor-AI/src/worker/routes/generate.ts
@SlideTutor-AI/src/worker/routes/parse.ts
@SlideTutor-AI/api/lib/env.ts
@SlideTutor-AI/api/lib/generateService.ts
@SlideTutor-AI/api/lib/azureParse.ts
@SlideTutor-AI/api/security.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cloudflare D1 quota bindings, usage identity, and successful-only daily usage persistence</name>
  <files>SlideTutor-AI/wrangler.jsonc, SlideTutor-AI/src/worker/index.ts, SlideTutor-AI/api/lib/env.ts, SlideTutor-AI/api/lib/parser/usageIdentity.ts, SlideTutor-AI/api/lib/parser/usageStore.ts, SlideTutor-AI/migrations/001_parser_usage_daily.sql, SlideTutor-AI/api/parserUsage.test.ts</files>
  <read_first>
    - SlideTutor-AI/wrangler.jsonc
    - SlideTutor-AI/src/worker/index.ts
    - SlideTutor-AI/api/lib/env.ts
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the quota identity is derived from client IP plus `USAGE_HASH_SECRET`, not from raw IP or `API_TOKEN_SECRET`.
    - Test 2: daily usage reads as `used`, `limit`, and `remaining` from a D1-backed store keyed by the natural-day `date_key`.
    - Test 3: usage increments only after a simulated successful parser result and does not change for failed or skipped parser attempts.
  </behavior>
  <action>Add a D1 binding named `PARSER_USAGE_DB` to `SlideTutor-AI/wrangler.jsonc`, plus Worker env support for `PARSER_USAGE_DB` and `USAGE_HASH_SECRET` in `SlideTutor-AI/src/worker/index.ts` and `SlideTutor-AI/api/lib/env.ts`. Create `SlideTutor-AI/migrations/001_parser_usage_daily.sql` with a `parser_usage_daily` table containing `usage_key TEXT PRIMARY KEY`, `date_key TEXT NOT NULL`, `usage_count INTEGER NOT NULL DEFAULT 0`, and `updated_at TEXT NOT NULL`. Create `SlideTutor-AI/api/lib/parser/usageIdentity.ts` to build a stable `usage_key` from `clientIp`, `dateKey`, and `USAGE_HASH_SECRET`. Create `SlideTutor-AI/api/lib/parser/usageStore.ts` with concrete functions for `getParserUsageSummary`, `canUsePlatformParser`, and `recordSuccessfulPlatformParse`, all using limit `10` and returning exact `used`, `remaining`, and `limit` values. Add `SlideTutor-AI/api/parserUsage.test.ts` to prove successful-only deduction, daily reset by `date_key`, and secret-backed hash generation.</action>
  <acceptance_criteria>
    - `rg -n "PARSER_USAGE_DB|USAGE_HASH_SECRET" SlideTutor-AI/wrangler.jsonc SlideTutor-AI/src/worker/index.ts SlideTutor-AI/api/lib/env.ts` finds the new binding and secret support.
    - `rg -n "CREATE TABLE parser_usage_daily|usage_count|date_key" SlideTutor-AI/migrations/001_parser_usage_daily.sql` finds the D1 schema fields.
    - `rg -n "getParserUsageSummary|canUsePlatformParser|recordSuccessfulPlatformParse" SlideTutor-AI/api/lib/parser/usageStore.ts` finds the daily usage service helpers.
    - `rg -n "USAGE_HASH_SECRET|successful-only|date_key|remaining" SlideTutor-AI/api/parserUsage.test.ts` finds direct coverage for the locked quota rules.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/parserUsage.test.ts</automated>
  </verify>
  <done>The Worker runtime knows about D1-backed parser usage truth, anonymous usage identity is secret-derived, and daily parser quota can be read and incremented safely for the current low-user stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce a shared parser access layer and route integration with degrade metadata</name>
  <files>SlideTutor-AI/api/lib/parser/provider.ts, SlideTutor-AI/api/lib/parser/azureProvider.ts, SlideTutor-AI/api/lib/parser/accessService.ts, SlideTutor-AI/api/lib/generateService.ts, SlideTutor-AI/src/worker/routes/generate.ts, SlideTutor-AI/src/worker/routes/parse.ts, SlideTutor-AI/src/worker/routes/parser-usage.ts, SlideTutor-AI/api/parserAccess.test.ts, SlideTutor-AI/test/workers/parse-route.worker.test.ts</files>
  <read_first>
    - SlideTutor-AI/api/lib/azureParse.ts
    - SlideTutor-AI/api/lib/generateService.ts
    - SlideTutor-AI/src/worker/routes/generate.ts
    - SlideTutor-AI/src/worker/routes/parse.ts
    - SlideTutor-AI/src/worker/index.ts
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-CONTEXT.md
    - .planning/phases/05-parser-bootstrap-and-provider-abstraction/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `/api/parse` and integrated explain parsing both call the same parser access layer instead of importing Azure directly.
    - Test 2: over-quota explain requests continue and mark the response with `x-slidetutor-parse-mode: degraded` instead of failing.
    - Test 3: successful parser calls return `x-slidetutor-parse-mode: normal` and increment quota, while failed or skipped calls do not increment quota.
    - Test 4: Settings can fetch exact parser usage from a new Worker route without learning any identity details.
  </behavior>
  <action>Create `SlideTutor-AI/api/lib/parser/provider.ts` with a minimal parser provider contract that returns normalized layout blocks for a `base64Image`. Create `SlideTutor-AI/api/lib/parser/azureProvider.ts` as the first provider implementation by moving the current Azure-specific call path behind that contract. Create `SlideTutor-AI/api/lib/parser/accessService.ts` to combine quota checks, provider execution, and result semantics into one result shape containing `parseMode: "normal" | "degraded"`, optional `layoutBlocks`, and a `usageSummary` snapshot. Update `SlideTutor-AI/api/lib/generateService.ts` so explain-task integrated parsing uses `accessService` instead of calling `performAzureAnalysis(...)` directly; when parser access is exhausted, keep explain generation running with no `layoutBlocks` and carry `parseMode: "degraded"` forward without changing prompt-building contracts. Update `SlideTutor-AI/src/worker/routes/generate.ts` to emit `x-slidetutor-parse-mode` and `x-slidetutor-parser-remaining` headers on the streaming response. Update `SlideTutor-AI/src/worker/routes/parse.ts` to use the same access layer and return structured usage fields. Add `SlideTutor-AI/src/worker/routes/parser-usage.ts` and register `/api/parser-usage` in `SlideTutor-AI/src/worker/index.ts`; its JSON response must include `used`, `remaining`, and `limit` for the current anonymous user and natural day. Add `SlideTutor-AI/api/parserAccess.test.ts` and `SlideTutor-AI/test/workers/parse-route.worker.test.ts` to prove the shared access layer, degrade-not-block behavior, and usage-endpoint contract.</action>
  <acceptance_criteria>
    - `rg -n "parseMode|usageSummary|layoutBlocks" SlideTutor-AI/api/lib/parser/accessService.ts SlideTutor-AI/api/lib/generateService.ts` finds the shared access result contract.
    - `rg -n "x-slidetutor-parse-mode|x-slidetutor-parser-remaining" SlideTutor-AI/src/worker/routes/generate.ts` finds the explicit degrade metadata headers.
    - `rg -n "/api/parser-usage|used|remaining|limit" SlideTutor-AI/src/worker/routes/parser-usage.ts SlideTutor-AI/src/worker/index.ts` finds the settings usage route and response fields.
    - `rg -n "degraded|normal|successful parser call|/api/parser-usage" SlideTutor-AI/api/parserAccess.test.ts SlideTutor-AI/test/workers/parse-route.worker.test.ts` finds test coverage for the phase-critical behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/parserAccess.test.ts && npm run test:workers -- test/workers/parse-route.worker.test.ts</automated>
  </verify>
  <done>The backend exposes one shared parser access layer, explicit usage truth, and degrade metadata that later frontend work can consume without touching teaching logic.</done>
</task>

</tasks>

<verification>
Run `npm test -- api/parserUsage.test.ts api/parserAccess.test.ts` and `npm run test:workers -- test/workers/parse-route.worker.test.ts`.
</verification>

<success_criteria>
Phase 05 has a backend parser access foundation that keeps early document parsing friction low while enforcing explicit server-side limits, provider abstraction, and degrade-not-block behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-parser-bootstrap-and-provider-abstraction/05-01-SUMMARY.md`
</output>
