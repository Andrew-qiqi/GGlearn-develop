---
phase: 04-byok-first-access-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SlideTutor-AI/src/config/models.ts
  - SlideTutor-AI/src/store/uiStore.ts
  - SlideTutor-AI/src/store/uiStore.test.ts
  - SlideTutor-AI/src/components/SettingsModal.tsx
  - SlideTutor-AI/src/lib/api/apiClient.ts
  - SlideTutor-AI/src/lib/api/apiClient.test.ts
  - SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts
  - SlideTutor-AI/src/hooks/useChunkRegenerate.test.ts
autonomous: true
requirements:
  - BYOK-01
  - BYOK-02
  - BYOK-03
must_haves:
  truths:
    - "The frontend stores model choice separately from access credentials so future hosted access can coexist with BYOK."
    - "OpenAI-compatible BYOK is configured through one shared shape instead of duplicating provider-specific credential forms."
    - "Existing teaching flows keep sending the same task payloads, with access metadata added without changing teaching logic."
  artifacts:
    - path: "SlideTutor-AI/src/store/uiStore.ts"
      provides: "Persisted BYOK access settings with legacy-safe initialization."
    - path: "SlideTutor-AI/src/components/SettingsModal.tsx"
      provides: "User-facing BYOK configuration UI for Gemini and OpenAI-compatible access."
    - path: "SlideTutor-AI/src/lib/api/apiClient.ts"
      provides: "Shared request contract that attaches normalized access metadata to /api/generate calls."
---

<objective>
Create the Phase 04 frontend access layer so a user can configure BYOK credentials locally and all generation flows send one normalized access contract to the backend.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persisted BYOK access state with legacy-safe migration</name>
  <files>SlideTutor-AI/src/config/models.ts, SlideTutor-AI/src/store/uiStore.ts, SlideTutor-AI/src/store/uiStore.test.ts</files>
  <behavior>
    - Legacy saved provider selections such as qwen/doubao do not crash initialization.
    - UI state persists a normalized access shape that can support current BYOK and future hosted mode.
    - OpenAI-compatible access is represented as one family even when the UI offers preset endpoints.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the BYOK settings UI without touching teaching behavior</name>
  <files>SlideTutor-AI/src/components/SettingsModal.tsx, SlideTutor-AI/src/config/models.ts</files>
  <behavior>
    - Gemini users can save their API key locally.
    - OpenAI-compatible users can choose a preset endpoint or custom base URL and set a model id.
    - The modal explains that parsing remains platform-funded in the current phase.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Send normalized access metadata through the shared API client</name>
  <files>SlideTutor-AI/src/lib/api/apiClient.ts, SlideTutor-AI/src/lib/api/apiClient.test.ts, SlideTutor-AI/src/hooks/useSlideAnalysis.test.ts, SlideTutor-AI/src/hooks/useChunkRegenerate.test.ts</files>
  <behavior>
    - Existing hooks keep calling one shared `apiGenerate(...)` helper.
    - Requests now include a normalized access payload for Gemini or OpenAI-compatible BYOK.
    - Hook tests prove the teaching requests still target the same tasks and include the new access metadata.
  </behavior>
</task>

</tasks>

<verification>
Run `npm test -- src/store/uiStore.test.ts src/lib/api/apiClient.test.ts src/hooks/useSlideAnalysis.test.ts src/hooks/useChunkRegenerate.test.ts`.
</verification>

<output>
After completion, create `.planning/phases/04-byok-first-access-layer/04-01-SUMMARY.md`
</output>
