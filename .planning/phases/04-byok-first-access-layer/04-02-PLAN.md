---
phase: 04-byok-first-access-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - SlideTutor-AI/api/lib/env.ts
  - SlideTutor-AI/api/lib/generateService.ts
  - SlideTutor-AI/api/security.test.ts
  - SlideTutor-AI/test/workers/generate-stream.worker.test.ts
  - SlideTutor-AI/README.md
  - docs/backend/api-design.md
  - docs/frontend/architecture.md
  - docs/frontend/data-flow.md
  - docs/changelog/CHANGELOG_TECH.md
autonomous: true
requirements:
  - BYOK-01
  - BYOK-02
  - BYOK-03
must_haves:
  truths:
    - "The backend can execute Gemini BYOK and OpenAI-compatible BYOK without forcing platform-hosted inference."
    - "OpenAI-compatible requests use one adapter path with request-supplied base URL instead of provider-specific forks."
    - "Legacy env-driven paths remain available so current deployments do not break during the migration window."
  artifacts:
    - path: "SlideTutor-AI/api/lib/generateService.ts"
      provides: "Normalized provider routing and credential resolution for BYOK-first generation."
    - path: "SlideTutor-AI/test/workers/generate-stream.worker.test.ts"
      provides: "Worker coverage for normalized BYOK request forwarding."
    - path: "docs/backend/api-design.md"
      provides: "Documented generate request contract for BYOK access metadata."
---

<objective>
Finish Phase 04 by teaching the backend how to consume the normalized BYOK payload while preserving the current teaching contracts and deployment safety.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize provider routing and credential resolution in generateService</name>
  <files>SlideTutor-AI/api/lib/env.ts, SlideTutor-AI/api/lib/generateService.ts, SlideTutor-AI/api/security.test.ts</files>
  <behavior>
    - Gemini may use a user-supplied API key or the existing env secret.
    - OpenAI-compatible uses one adapter path with request-supplied base URL and key, while old qwen/doubao env routes still work.
    - Missing or mismatched BYOK inputs return route-friendly validation errors.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove worker forwarding and write the operator docs</name>
  <files>SlideTutor-AI/test/workers/generate-stream.worker.test.ts, SlideTutor-AI/README.md, docs/backend/api-design.md, docs/frontend/architecture.md, docs/frontend/data-flow.md, docs/changelog/CHANGELOG_TECH.md</files>
  <behavior>
    - Worker tests show `/api/generate` forwards the normalized access payload.
    - Docs explain that BYOK keys stay local to the browser in this phase and parsing remains platform-funded.
    - Docs explain the future coexistence boundary with hosted access without pretending hosted mode already exists.
  </behavior>
</task>

</tasks>

<verification>
Run `npm test -- api/security.test.ts test/workers/generate-stream.worker.test.ts` plus the broader `npm test`, `npm run test:workers`, and `npm run lint`.
</verification>

<output>
After completion, create `.planning/phases/04-byok-first-access-layer/04-02-SUMMARY.md`
</output>
