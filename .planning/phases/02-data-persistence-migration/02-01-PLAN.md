---
phase: 02-data-persistence-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [SlideTutor-AI/src/lib/db.ts, SlideTutor-AI/src/lib/migrate.ts]
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "IndexedDB schema includes 'appSettings' store"
    - "SavedPDF interface includes 'lastReadPage' field"
    - "A migration script exists to transfer localStorage data to IndexedDB"
  artifacts:
    - path: "SlideTutor-AI/src/lib/db.ts"
      provides: "Updated DB schema and settings accessors"
    - path: "SlideTutor-AI/src/lib/migrate.ts"
      provides: "LocalStorage to IndexedDB migration logic"
  key_links:
    - from: "SlideTutor-AI/src/lib/migrate.ts"
      to: "SlideTutor-AI/src/lib/db.ts"
      via: "setSetting and updatePDFLastPage calls"
---

<objective>
Update the IndexedDB schema to support application settings and create a migration script to transition from localStorage.

Purpose: Robust storage for settings and page tracking, and a safe path for existing users.
Output: Enhanced `db.ts` and a new `migrate.ts` utility.
</objective>

<execution_context>
@/Users/qiqicute/Documents/z_cqmeng_file/local_repository/SlideTutor-AI-main/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/qiqicute/Documents/z_cqmeng_file/local_repository/SlideTutor-AI-main/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SlideTutor-AI/src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IndexedDB schema and accessors</name>
  <files>SlideTutor-AI/src/lib/db.ts</files>
  <action>
    - Update `SavedPDF` interface to include `lastReadPage?: number`.
    - Bump `DB_VERSION` to 2.
    - Update `getDB()`: Add `appSettings` object store creation in `onupgradeneeded`.
    - Implement new exported functions:
      - `getSetting<T>(key: string, defaultValue: T): Promise<T>`
      - `setSetting(key: string, value: any): Promise<void>`
      - `updatePDFLastPage(id: string, page: number): Promise<void>`
    - Update `savePDF` to ensure it doesn't overwrite existing `lastReadPage` if updating an existing entry, OR just allow it to be optional.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Database schema updated and accessors implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement migration script</name>
  <files>SlideTutor-AI/src/lib/migrate.ts</files>
  <action>
    - Create `migrateLocalStorageToIndexedDB()` function.
    - Logic:
      1. Check for `localStorage` keys: `slide_tutor_model`, `slide_tutor_language`, `theme`, `slide_tutor_focus_mode`.
      2. If found, use `setSetting` to store them in IndexedDB.
      3. Scan `localStorage` for keys matching `pdf-page-*`.
      4. For each found, parse PDF ID and page number, then use `updatePDFLastPage` to store in IndexedDB.
      5. Only clear `localStorage` keys after confirming all migrations were successful (or at least attempted).
    - Ensure it's idempotent (calling twice shouldn't break anything).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Migration script implemented and ready for integration.</done>
</task>

</tasks>

<verification>
Check `src/lib/db.ts` for schema changes and `src/lib/migrate.ts` for migration logic.
</verification>

<success_criteria>
- Schema supports settings and page tracking.
- Migration script handles all target localStorage keys.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-persistence-migration/02-01-SUMMARY.md`
</output>
