---
phase: 02-data-persistence-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [SlideTutor-AI/src/store/uiStore.ts, SlideTutor-AI/src/store/tutorStore.ts, SlideTutor-AI/src/hooks/usePdfLibrary.ts, SlideTutor-AI/src/App.tsx, SlideTutor-AI/src/components/ThemeToggle.tsx]
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Application initializes settings from IndexedDB"
    - "PDF last read page is restored using IndexedDB metadata"
    - "LocalStorage is cleared after successful migration"
  artifacts:
    - path: "SlideTutor-AI/src/store/uiStore.ts"
      provides: "Async initialized UI store"
    - path: "SlideTutor-AI/src/store/tutorStore.ts"
      provides: "Async initialized tutor store"
  key_links:
    - from: "SlideTutor-AI/src/App.tsx"
      to: "SlideTutor-AI/src/lib/migrate.ts"
      via: "Initial migration call"
    - from: "SlideTutor-AI/src/hooks/usePdfLibrary.ts"
      to: "SlideTutor-AI/src/lib/db.ts"
      via: "Loading lastReadPage"
---

<objective>
Refactor application stores and components to use IndexedDB as the primary source of truth for settings and persistent state.

Purpose: Completes the transition to robust IndexedDB-based storage.
Output: Updated Zustand stores and integrated UI components.
</objective>

<execution_context>
@/Users/qiqicute/Documents/z_cqmeng_file/local_repository/SlideTutor-AI-main/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/qiqicute/Documents/z_cqmeng_file/local_repository/SlideTutor-AI-main/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SlideTutor-AI/src/store/uiStore.ts
@SlideTutor-AI/src/store/tutorStore.ts
@SlideTutor-AI/src/hooks/usePdfLibrary.ts
@SlideTutor-AI/src/App.tsx
@SlideTutor-AI/src/components/ThemeToggle.tsx
@SlideTutor-AI/src/lib/db.ts
@SlideTutor-AI/src/lib/migrate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async Store Initialization</name>
  <files>SlideTutor-AI/src/store/uiStore.ts, SlideTutor-AI/src/store/tutorStore.ts</files>
  <action>
    - Update `useUiStore` and `useTutorStore` to support async initialization.
    - Logic:
      1. Initialize stores with default values.
      2. Add an `init()` action to each store that fetches settings from `getSetting` in `db.ts`.
      3. Update the `set` actions for settings (e.g., `setSelectedModel`, `setIsFocusMode`) to use `setSetting` from `db.ts` instead of `localStorage`.
    - Ensure `SelectedModel` and other types are maintained.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Zustand stores now backed by IndexedDB.</done>
</task>

<task type="auto">
  <name>Task 2: UI Wiring and Migration Integration</name>
  <files>SlideTutor-AI/src/App.tsx, SlideTutor-AI/src/hooks/usePdfLibrary.ts, SlideTutor-AI/src/components/ThemeToggle.tsx</files>
  <action>
    - In `App.tsx`:
      1. Call `migrateLocalStorageToIndexedDB()` on application mount.
      2. Call `init()` on both stores (UI and Tutor) to load settings.
      3. Update the `pageNumber` change effect to use `updatePDFLastPage` from `db.ts` instead of `localStorage`.
    - In `usePdfLibrary.ts`:
      1. Use the `lastReadPage` from the `pdf` object returned by `getPDF(id)` instead of checking `localStorage`.
    - In `ThemeToggle.tsx`:
      1. Update to use `getSetting` and `setSetting` for theme management.
      2. Handle initial async load of theme gracefully.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Application integrated with IndexedDB and migration logic triggered.</done>
</task>

</tasks>

<verification>
Verify persistence of settings and PDF page tracking after page refresh. Ensure localStorage is eventually empty.
</verification>

<success_criteria>
- Theme persists across refresh.
- AI model and language settings persist.
- PDF page tracking is maintained using IndexedDB.
- Migration from LocalStorage occurs once successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-persistence-migration/02-02-SUMMARY.md`
</output>
