---
phase: 07-china-user-operational-fit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SlideTutor-AI/api/lib/generateService.ts
  - SlideTutor-AI/api/lib/generateService.platform.test.ts
  - SlideTutor-AI/src/lib/api/apiClient.ts
  - SlideTutor-AI/src/components/SettingsModal.tsx
  - SlideTutor-AI/src/components/SettingsModal.test.tsx
  - SlideTutor-AI/src/components/settings/PlatformApiSection.tsx
  - SlideTutor-AI/src/components/settings/PlatformApiSection.test.tsx
  - docs/user_guide/access-modes.md
autonomous: true
requirements:
  - CN-01
  - CN-03
must_haves:
  truths:
    - "China-based requests must not assume Gemini API availability."
    - "Users still choose `My API` or `Platform API`; the product must not auto-switch providers for them."
    - "Access guidance must stay intentionally light and must not turn the settings UI into a provider recommendation wall."
  artifacts:
    - path: "SlideTutor-AI/api/lib/generateService.ts"
      provides: "Normalized provider-region error contract for real China-user failures."
    - path: "SlideTutor-AI/src/components/settings/PlatformApiSection.tsx"
      provides: "Low-noise access-mode guidance that keeps user choice explicit."
    - path: "docs/user_guide/access-modes.md"
      provides: "Repo-native explanation of `My API` vs `Platform API` and region/provider availability assumptions."
---

<objective>
Normalize China-user access failures and make the existing access-path assumptions explicit without adding noisy recommendation UX.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize region/provider-blocked failures into a stable backend and client contract</name>
  <files>SlideTutor-AI/api/lib/generateService.ts, SlideTutor-AI/api/lib/generateService.platform.test.ts, SlideTutor-AI/src/lib/api/apiClient.ts</files>
  <read_first>
    - SlideTutor-AI/api/lib/generateService.ts
    - SlideTutor-AI/api/lib/generateService.platform.test.ts
    - SlideTutor-AI/src/lib/api/apiClient.ts
    - .planning/phases/07-china-user-operational-fit/07-CONTEXT.md
    - .planning/phases/07-china-user-operational-fit/07-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Region/provider-blocked failures become an explicit app-level error instead of a generic upstream failure.
    - The error contract stays compatible with existing hook error handling.
    - The runtime does not auto-switch providers or downgrade user choice silently.
  </behavior>
  <action>Add a small provider-failure normalization helper inside `SlideTutor-AI/api/lib/generateService.ts` that inspects upstream model errors and maps Gemini region-blocked failures such as `User location is not supported for the API use` into a stable `GenerateRouteError` with code `PROVIDER_REGION_UNSUPPORTED` and a user-safe message. Preserve the user's selected `providerId`; do not auto-fallback to OpenAI-compatible or `Platform API`. Update `SlideTutor-AI/api/lib/generateService.platform.test.ts` with coverage for the normalized code path, and update `SlideTutor-AI/src/lib/api/apiClient.ts` so `readApiError(...)` preserves the new error code cleanly for downstream hooks.</action>
  <acceptance_criteria>
    - `rg -n "PROVIDER_REGION_UNSUPPORTED" SlideTutor-AI/api/lib/generateService.ts SlideTutor-AI/api/lib/generateService.platform.test.ts SlideTutor-AI/src/lib/api/apiClient.ts` finds the normalized error code in service, tests, and client handling.
    - `npm test -- api/lib/generateService.platform.test.ts src/lib/api/apiClient.test.ts` exits 0.
    - `rg -n "auto-switch|fallback to platform" SlideTutor-AI/api/lib/generateService.ts` finds no hidden provider auto-switch logic.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- api/lib/generateService.platform.test.ts src/lib/api/apiClient.test.ts</automated>
  </verify>
  <done>China-user provider/region failures are explicit, test-covered, and still preserve user choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add low-noise access guidance and a repo-native access-modes guide</name>
  <files>SlideTutor-AI/src/components/SettingsModal.tsx, SlideTutor-AI/src/components/SettingsModal.test.tsx, SlideTutor-AI/src/components/settings/PlatformApiSection.tsx, SlideTutor-AI/src/components/settings/PlatformApiSection.test.tsx, docs/user_guide/access-modes.md</files>
  <read_first>
    - SlideTutor-AI/src/components/SettingsModal.tsx
    - SlideTutor-AI/src/components/SettingsModal.test.tsx
    - SlideTutor-AI/src/components/settings/PlatformApiSection.tsx
    - docs/frontend/architecture.md
    - docs/frontend/data-flow.md
    - .planning/phases/07-china-user-operational-fit/07-CONTEXT.md
    - .planning/phases/07-china-user-operational-fit/07-RESEARCH.md
  </read_first>
  <behavior>
    - Users can still choose `My API` or `Platform API` directly.
    - The UI adds only one concise explanation of provider/region variability instead of a long recommendation block.
    - The repo gains a stable user guide for access modes, parser ownership, and region/provider caveats.
  </behavior>
  <action>Update `SlideTutor-AI/src/components/settings/PlatformApiSection.tsx` and, if needed, `SlideTutor-AI/src/components/SettingsModal.tsx` with one concise note that model/provider availability can vary by region and that users can either choose another `My API` provider or use `Platform API` if their current provider is unavailable. Keep the note short and do not force a recommendation. Add `SlideTutor-AI/src/components/settings/PlatformApiSection.test.tsx` and extend `SlideTutor-AI/src/components/SettingsModal.test.tsx` to cover the new copy. Create `docs/user_guide/access-modes.md` that explains `My API` vs `Platform API`, local credential storage, platform-managed parser ownership, and the fact that Gemini availability should not be assumed for China-based users.</action>
  <acceptance_criteria>
    - `rg -n "availability can vary by region|another My API provider|Platform API" SlideTutor-AI/src/components/settings/PlatformApiSection.tsx SlideTutor-AI/src/components/SettingsModal.tsx` finds the new low-noise guidance.
    - `test -f docs/user_guide/access-modes.md` succeeds.
    - `rg -n "My API|Platform API|Gemini|OpenAI-compatible|parser" docs/user_guide/access-modes.md` finds the required access-mode guide topics.
    - `npm test -- src/components/SettingsModal.test.tsx src/components/settings/PlatformApiSection.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/SettingsModal.test.tsx src/components/settings/PlatformApiSection.test.tsx</automated>
  </verify>
  <done>The UI and docs explain access-mode reality clearly without adding noisy recommendation UX.</done>
</task>

</tasks>

<verification>
Run `npm test -- api/lib/generateService.platform.test.ts src/lib/api/apiClient.test.ts src/components/SettingsModal.test.tsx src/components/settings/PlatformApiSection.test.tsx` and `npm run lint`.
</verification>

<success_criteria>
Phase 07 can now represent China-user provider friction honestly: the user keeps explicit choice, region/provider failures become understandable, and the access model is documented in one stable user guide.
</success_criteria>

<output>
After completion, create `.planning/phases/07-china-user-operational-fit/07-01-SUMMARY.md`
</output>
